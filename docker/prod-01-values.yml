autoscaling:
  enabled: true
  minReplicas: 2
  maxReplicas: 4
ingress:
  enabled: true
  type: nginx
  tls:
    mailman-web:
      secretName: lists.uw.edu-ingress-cert
      hosts:
        - lists.uw.edu
    list01-mailman-web:
      secretName: list01.lists.uw.edu-ingress-cert
      hosts:
        - list01.lists.uw.edu
  hosts:
    mailman-web:
      host: lists.uw.edu
      paths:
        - "/"
    list01-mailman-web:
      host: list01.lists.uw.edu
      paths:
        - "/"
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt
securityPolicy:
  enabled: true
readiness:
  enabled: true
database:
  engine: postgres
  name: mailmanweb
  hostname: mailman-web-01-db-prod-service
  secretName: list01.lists.uw.edu-sql-secret
externalService:
  enabled: true
  name: mailman-web-01-db-prod-service
  type: ClusterIP
  serviceAddress: 172.18.1.44
  servicePort: 5432
repo: mailman-web
repoIdentifier: "01"
instance: prod
resources:
  limits:
    cpu: 500m
    memory: 1Gi
  requests:
    cpu: 50m
    memory: 256Mi
metrics:
  enabled: true
  serviceMonitor:
    enabled: true
  pushgateway:
    enabled: false
certs:
  mounted: true
  certPath: /certs/list01.lists.uw.edu.cert
  keyPath: /certs/lsit01.lists.uw.edu.key
  secretName: list01.lists.uw.edu-ic-certs
podVolumes:
  mailman-core-pvc-opt-mailman-web-data:
    # no "volume:" definition since it was defined in prod-01-values.yml
    persistentVolumeClaim:
      claimName: mailman-core-01-prod-prod-pvc-opt-mailman-web-data-mailman-core-01-prod-prod-0
    mount:
      mountPath: /opt/mailman-web-data
      subPath: "01"
    containers: [ base ]
environmentVariables:
  - name: ENV
    value: prod
  - name: AUTH
    value: SAML
  - name: LOGIN_URL
    value: saml_login
  - name: LOGOUT_URL
    value: saml_logout
  - name: MAILMAN_REST_URL
    value: http://mailman-core-01
  - name: MAILMAN_ARCHIVER_FROM
    value: mailman-core-01
  - name: CLUSTER_CNAME
    value: list01.lists.uw.edu
  - name: SAML_ENTITY_ID
    value: https://list01.lists.uw.edu/sso/
  - name: POSTORIUS_TEMPLATE_BASE_URL
    value: https://list01.lists.uw.edu/
externalSecrets:
  enabled: true
  secrets:
    - name: list01.lists.uw.edu-shared-secrets
      externalKey: mailman/shared/prod/secrets
      data:
        - name: mailman-rest-user
          property: mailman-admin-user
        - name: mailman-rest-password
          property: mailman-admin-password
        - name: hyperkitty-api-key
          property: hyperkitty-api-key
    - name: list01.lists.uw.edu-secrets
      externalKey: mailman/web/prod/secrets
      data:
        - name: django-secret
          property: django-secret
        - name: email-host
          property: email-host
        - name: default-from-email
          property: default-from-email
        - name: mailman2-rest-key
          property: mailman2-rest-key
    - name: list01.lists.uw.edu-sql-secret
      externalKey: mailman/web/prod/sql-secret
      data:
        - name: username
          property: sql_user
        - name: password
          property: sql_pass
    - name: list01.lists.uw.edu-ic-certs
      externalKey: mailman/web/prod/01/ic-certs
      data:
        - name: list01.lists.uw.edu.cert
          property: cert
        - name: list01.lists.uw.edu.key
          property: key
    - name: list01.lists.uw.edu-uw-idp-cert
      externalKey: idp-cert
      data:
        - name: uw-idp-cert
          property: cert
environmentVariablesSecrets:
  djangoSecret:
    name: DJANGO_SECRET
    secretName: list01.lists.uw.edu-secrets
    secretKey: django-secret
  emailHost:
    name: EMAIL_HOST
    secretName: list01.lists.uw.edu-secrets
    secretKey: email-host
  defaultFromEmail:
    name: DEFAULT_FROM_EMAIL
    secretName: list01.lists.uw.edu-secrets
    secretKey: default-from-email
  mailmanAdminUser:
    name: MAILMAN_REST_USER
    secretName: list01.lists.uw.edu-shared-secrets
    secretKey: mailman-rest-user
  mailmanAdminPassword:
    name: MAILMAN_REST_PASSWORD
    secretName: list01.lists.uw.edu-shared-secrets
    secretKey: mailman-rest-password
  hyperkittyAPIKey:
    name: HYPERKITTY_API_KEY
    secretName: list01.lists.uw.edu-shared-secrets
    secretKey: hyperkitty-api-key
  SAMLServiceProviderCert:
    name: SP_CERT
    secretName: list01.lists.uw.edu-ic-certs
    secretKey: lists.uw.edu.cert
  SAMLIDPCert:
    name: IDP_CERT
    secretName: list01.lists.uw.edu-uw-idp-cert
    secretKey: uw-idp-cert
